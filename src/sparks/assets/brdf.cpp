#include "sparks/assets/brdf.h"

namespace sparks {

glm::vec3 brdf(glm::vec3 &idir, glm::vec3 &odir, const Material &material) {
  glm::vec3 f(1.0f, 1.0f, 1.0f);
  return f;
}

}  // namespace sparks
